apiVersion: v1
kind: Service
metadata:
  name: {{ include "argocd-ingress.fullname" . }}
  labels:
    {{- include "argocd-ingress.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argocd-ingress.selectorLabels" . | nindent 4 }}
